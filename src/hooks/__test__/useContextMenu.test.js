import useContextMenu from '../useContextMenu';

describe('Testing useContextMenu hook', () => {
  it.todo('displays context menu');
  it.todo('hides context menu on left click');
  it.todo('hides context menu on click outside');
});
